[package]
name = "scholarship_fund"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[features]
default = ["fund"]
fund = []
registry = []

[dependencies]
soroban-sdk = { version = "27.0.0-rc.1", features = ["alloc"] }
[dev-dependencies]
soroban-sdk = { version = "27.0.0-rc.1", features = ["testutils", "alloc"] }
[profile.release]
opt-level = "z"
overflow-checks = false
debug = 0
strip = "symbols"
debug-assertions = false
panic = "abort"
codegen-units = 1
lto = true
